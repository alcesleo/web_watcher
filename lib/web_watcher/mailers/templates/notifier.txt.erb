Value changed from:

"<%= request1.value %>"

To:

"<%= request2.value %>"

The change took place between <%= request1.created_at %> and <%= request2.created_at %>.

<%= ENV.fetch("HOST_ADDRESS") + Web.routes.watcher_path(watcher.id) %>
